{
  "name": "prompt-lab",
  "private": true,
  "version": "1.5.0",
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix prompt-lab-extension",
    "build": "npm run build --prefix prompt-lab-extension",
    "test": "npm test --prefix prompt-lab-extension",
    "build:landing": "node scripts/publish-landing.mjs",
    "publish:landing": "npm run build:landing"
  }
}
