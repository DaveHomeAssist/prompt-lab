name: Merge Gate

on:
  pull_request:

permissions:
  contents: read

jobs:
  merge-gate:
    name: merge-gate
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Establish required merge context
        run: echo "merge-gate is available for every pull request"
